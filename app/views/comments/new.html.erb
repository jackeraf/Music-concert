<h1>Type your comment here! </h1>


<%= form_for [@concert, @comment] do |f| %> 
<!-- Form for with two instance variables: is because in routes comments is nested inside concert url (nested resource.) -->

  <%= f.label :user_name %>
  <%= f.text_field :user_name %>
  <br>
  <%= f.label :comment %>
	<%= f.text_field :comment %>

	<%= f.submit "Submit" %>

<% end %>


